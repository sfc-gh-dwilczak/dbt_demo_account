version: 2

models:
  - name: src_tasty_bytes__order_detail
    config:
      contract:
        enforced: true
    
    columns:
      - name: order_detail_id
        data_type: number

      - name: order_id
        data_type: number

      - name: menu_item_id
        data_type: number

      - name: discount_id
        data_type: varchar

      - name: line_number
        data_type: number

      - name: quantity
        data_type: number

      - name: unit_price
        data_type: number

      - name: price
        data_type: number

      - name: order_item_discount_amount
        data_type: varchar
