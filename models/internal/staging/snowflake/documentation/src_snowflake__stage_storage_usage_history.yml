version: 2

models:
  - name: src_snowflake__stage_storage_usage_history
    config:
      contract:
        enforced: true
    
    columns:
      - name: usage_date
        data_type: date

      - name: average_stage_bytes
        data_type: number
